<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.seckill.dao.UserMapper">

  <select id="getById" resultType="User">
    SELECT id,
           nickname,
           password,
           salt,
           head,
           register_date,
           last_login_date,
           login_count
    FROM miaosha_user
    WHERE id = #{id}
  </select>

  <insert id="insert" parameterType="User">
    INSERT INTO miaosha_user
      (id, nickname, password, salt, head, register_date, last_login_date, login_count)
    VALUES
      (#{id}, #{nickname}, #{password}, #{salt}, #{head}, #{registerDate},
       #{lastLoginDate}, #{loginCount})
  </insert>

</mapper>