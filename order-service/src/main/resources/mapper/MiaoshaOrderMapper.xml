<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.order.dao.MiaoshaOrderMapper">

  <select id="getByUserIdAndGoodsId" resultType="MiaoshaOrder">
    SELECT id, user_id, order_id, goods_id
    FROM miaosha_order
    WHERE user_id = #{userId}
      AND goods_id = #{goodsId}
  </select>

  <insert id="insert" parameterType="MiaoshaOrder" useGeneratedKeys="true" keyProperty="id">
    INSERT INTO miaosha_order (user_id, order_id, goods_id)
    VALUES (#{userId}, #{orderId}, #{goodsId})
  </insert>

</mapper>
